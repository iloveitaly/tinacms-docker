# TinaCMS Docker Configuration

# GitHub Configuration (Required)
# Generate a Personal Access Token at: https://github.com/settings/tokens
# Required scope: repo (full control of private repositories)
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_your_token_here
GITHUB_OWNER=your-github-username
GITHUB_REPO=your-repo-name
GITHUB_BRANCH=main

# HTTP Basic Auth Configuration (Required for production)
# Simple username/password authentication
TINA_ADMIN_USERNAME=admin
TINA_ADMIN_PASSWORD=your-secure-password-here

# Application Mode
# Set to "true" for local development (uses filesystem instead of MongoDB)
# Set to "false" for production (uses MongoDB and GitHub)
TINA_PUBLIC_IS_LOCAL=false

# MongoDB Configuration (Required for production)
# When using docker-compose, this is automatically set
# For external MongoDB, use: mongodb://username:password@host:port/database
MONGODB_URI=mongodb://mongodb:27017/tinacms

# Node Environment
NODE_ENV=production
